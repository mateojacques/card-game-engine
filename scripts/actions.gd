extends Node

# Every action that can affect the game goes here as a signal.
# Then we can send the signal from any node like: Actions.signal_name.emit();
# And we can receive the signal from any node and attach it a function to execute, with: Actions.signal_name.connect(function);
